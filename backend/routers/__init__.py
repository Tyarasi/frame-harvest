# Paket router FastAPI — satu file per domain (projects, cameras, capture,
# labels, split, dataset_prep, training_resnet, training_yolo, streaming).
# app.py cuma include_router semuanya, tidak ada route yang didefinisikan
# langsung di app.py selain "/" (health/serve index.html).
