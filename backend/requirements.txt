fastapi==0.115.0
uvicorn[standard]==0.30.6
opencv-python-headless==4.10.0.84
pyyaml==6.0.2

# Deteksi orang (bbox) pakai YOLOv8 — torch/torchvision HARUS dari index CPU-only
# di bawah, bukan PyPI default, kalau tidak ultralytics akan menarik build CUDA
# yang jauh lebih besar (~2-3GB vs ~300MB) dan bisa gagal karena versi tidak match.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu
ultralytics==8.4.127
